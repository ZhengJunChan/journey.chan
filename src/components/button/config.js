const white = '#fff'
const green = '#6bb551'

const text = 'Go'

const size = {
  big: {
    width: '215px',
    height: '60px'
  }
}

const textAlign = 'center'

const configs = {
  item: {
    text: text,
    color: white,
    textAlign: textAlign,
    width: size.big.width,
    backgroundColor: green,
    height: size.big.height,
    lineHeight: size.big.height
  }
}

export default configs
